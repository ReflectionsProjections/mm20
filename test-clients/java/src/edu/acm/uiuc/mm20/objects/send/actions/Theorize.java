package edu.acm.uiuc.mm20.objects.send.actions;

import edu.acm.uiuc.mm20.objects.send.Action;

public class Theorize extends Action {
	public Theorize (long person_id) {
		super("thoerize", person_id);
	}
}
